# CI status

The strict analyzer gate is intentionally kept enabled. Analyzer findings must be fixed in source rather than hidden with `continue-on-error` or broad analyzer ignores.

Current modernization targets are documented in `docs/ANALYZER_CLEANUP.md`.
