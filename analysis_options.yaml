# Analyzer configuration for Kadd.
# Keep Flutter's recommended lints enabled. Informational lints remain
# visible locally; CI controls whether infos are fatal explicitly.
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    unused_field: ignore

linter:
  rules:
    # Keep Flutter's recommended rules from flutter_lints.
