# Android project

This directory is intentionally kept in the repository so Flutter recognizes the Android platform source tree.

The CI build generates the Flutter Android project and applies the native Kadd additions from `android_additions/` before building the APK.